# Environment Variables (Optional)

# App Configuration
VITE_APP_NAME=Interview Haven
VITE_APP_VERSION=1.0.0

# API Configuration
# Leave unset in development: frontend uses /api and Vite proxies to http://localhost:5000
# For production (or if backend runs on another host/port), set e.g.:
# VITE_API_URL=http://localhost:5000

# Feature Flags
# VITE_ENABLE_ANALYTICS=false
# VITE_ENABLE_EMAIL_NOTIFICATIONS=false

# Note: Vite only exposes variables prefixed with VITE_
# Restart dev server after changing environment variables
